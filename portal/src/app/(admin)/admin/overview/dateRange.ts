// Moved to src/lib/dateRange.ts (shared with the marketer module);
// re-exported here so existing imports keep working.
export { resolveDateRange, resolveThisWeekRanges } from '@/lib/dateRange';
export type { ResolvedRange, ThisWeekRanges } from '@/lib/dateRange';
